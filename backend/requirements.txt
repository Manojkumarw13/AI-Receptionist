# AI Receptionist — Backend Requirements
# Install from this file when working inside the backend/ directory:
#   pip install -r requirements.txt
#
# BUG-25 FIX: Created this backend-specific requirements file to match README
# installation instructions: "cd backend && pip install -r requirements.txt"

# ── Core LangChain / AI ───────────────────────────────────────────────────────
langgraph==0.2.0
langchain_community==0.2.0
langchain_core==0.2.0
langchain-groq==0.1.0

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9        # For file upload (visitor photos)
python-jose[cryptography]>=3.3.0,<4.0.0  # JWT tokens
email-validator>=2.1.0         # Pydantic email validation
python-dotenv==1.0.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0

# ── Security ──────────────────────────────────────────────────────────────────
bcrypt==4.0.1

# ── Data Science / ML ─────────────────────────────────────────────────────────
pandas==2.1.0
scikit-learn==1.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
qrcode==7.4.2
pytz==2023.3
